# `statmt/cc100` still uses a dataset script; `datasets` 4.x rejects script-based loaders.
datasets>=2.18.0,<4.0.0
huggingface_hub>=0.22.0
# PyPI 3.0.6 fails on Python 3.11+ (regex); fixed on main (pre-release).
wikiextractor @ git+https://github.com/attardi/wikiextractor.git@ab8988ebfa9e4557411f3d4c0f4ccda139e18875
tqdm>=4.66.0
requests>=2.31.0
pandas>=2.0.0
matplotlib>=3.8.0
numpy>=1.26.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
sacremoses>=0.1.1
conllu>=4.5.0
torch>=2.0.0,<3.0.0